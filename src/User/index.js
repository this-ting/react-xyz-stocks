import React from 'react';
import { Box } from '@material-ui/core';
import Login from './User-login.js';

const User = () => {
  return (
    <Box>
      <Login />
    </Box>
  );
};

export default User;
