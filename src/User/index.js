import React from 'react';
import { Box } from '@material-ui/core';
import Login from './Login.js';

const User = () => {
  return (
    <Box>
      <Login />
    </Box>
  );
};

export default User;
